# Internation Inc Web Page

To test:
```bash
$ npm install -g bower
$ bower install
$ npm install
$ gulp serve
```

To compile:
```bash
$ npm install
$ npm install -g gulp
$ gulp
```

See `gulpfile.js` for task config.
